ات",""))}
    else if(page==="visits"){const rows=visibleVisits().map(v=>[v.id,v.contractId||"—",contractLabel(contracts.find(c=>c.id===v.contractId)||v),v.building?.name||"غير محدد",v.assignedName||"غير مسند",fmtDate(v.scheduledAt),badge(v.status),visitActions(v)]);c.innerHTML=head(session.role==="technician"?"زياراتي":"الزيارات","مواقع وجدولة الزيارات",canManage?'<button class="btn-primary" data-action="visit">زيارة جديدة</button>':"")+(rows.length?table(["الرقم","العقد","الطرف الثاني","الموقع","الفني","الموعد","الحالة","التقرير"],rows):empty("لا توجد زيارات",""))}
    else if(page==="meetings")c.innerHTML=meetingsPage();
    else if(page==="tracking")c.innerHTML=trackingPage();
    else if(page==="my-location")c.innerHTML=myLocationPage();
    else if(page==="team"){const myCo=activeOwnerCompany();const myIds=myCo?[...new Set([myCo.ownerId,...(myCo.ownerIds||[])].filter(Boolean))]:[ownerId()];const staffRows=staff.filter(s=>sameCompany(s)).map(s=>[s.name,s.identity,roles[s.role]||s.role,s.availability||"working"]);const admins=users.filter(u=>u.role==="company_admin"&&myIds.includes(u.companyOwnerId));const adminRows=admins.map(u=>[u.name,u.id,roles[u.role]||u.role,"نشط"]);const isOwner=session.role==="owner";const pendingInvites=adminInvites.filter(i=>i.companyOwnerId===ownerId()&&i.status==="pending");const myPending=adminInvites.filter(i=>i.userId===session.id&&i.status==="pending");let adminSection="";if(isOwner)adminSection+=`<section class="panel"><h3>إدارة المدراء</h3>${adminRows.length?tab