"use strict";
const assert=require("node:assert/strict"),r=require("../src/v2/reporting.cjs"),tx=require("../src/v2/transactions.cjs"),ctx={userId:"O1",role:"owner",tenantId:"C1"},state={misadJournalEntries:[],v2AccountingPeriods:[],v2Audit:[]};
assert.equal(tx.createSalesInvoice(state,{id:"SI1",clientId:"C1",contractId:"CONT1",date:"2026-06-01",net:1000},ctx).ok,true);assert.equal(tx.createPurchaseInvoice(state,{id:"PI1",supplierId:"S1",contractId:"CONT1",date:"2026-06-02",net:200},ctx).ok,true);const trial=r.trialBalance(state);assert.equal(amountOf(trial,"1320"),1000);assert.equal(r.incomeStatement(state).netIncome,800);assert.equal(r.balanceSheet(state).difference,0);assert.equal(r.aging(state,"misadCustomerInvoices","2026-10-01").buckets.over90,1000);assert.equal(r.contractProfitability(state,"CONT1").grossProfit,800);assert.equal(r.generate(state,"trial-balance").rows.length,trial.length);function amountOf(rows,id){return rows.find(row=>row.account===id).balance}console.log("v2 auditable reporting checks passed");
