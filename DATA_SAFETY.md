# حماية قاعدة البيانات أثناء النشر

قاعدة البيانات التشغيلية هي ملف JSON على القرص الدائم في Render، وليست ملفًا من ملفات المستودع. الإعداد المعتمد هو:

- `DATA_DIR=/var/data`
- `STORAGE_PATH=/var/data/storage.json`
- قرص Render باسم `ertiqaa-secure-data` ومثبت على `/var/data`
- `REQUIRE_PERSISTENT_STORAGE=1`

عند بدء الخادم تُنفذ ترقية غير إتلافية قبل استقبال الطلبات. الترقية:

1. ترفض التشغيل إذا كانت القاعدة الموجودة غير صالحة بدل التعامل معها كقاعدة فارغة.
2. تنشئ نسخة احتياطية موثقة قبل أي تعديل.
3. تضيف المفاتيح والحقول الناقصة فقط، ولا تستبدل أي سجل قائم.
4. تتحقق من عدم انخفاض أعداد السجلات ومن توازن قيود اليومية.
5. تكتب الملف بطريقة ذرّية حتى لا يترك انقطاع النشر ملفًا ناقصًا.
6. لا تضيف أي حساب أو حركة ضريبية.

`FORCE_SEED_STORAGE=1` معطل عمدًا ولا يستطيع استبدال البيانات بقالب المستودع. كما لا يجب ضبط `ALLOW_EMPTY_STORAGE_INIT=1` في نشر قائم؛ يستخدم فقط عند إنشاء خدمة جديدة مؤكدة لا تحتوي على بيانات.

## ترقية نسخة يدوية

```bash
npm run migrate:storage -- --file /path/to/storage.json
```

يحفظ الأمر نسخة ما قبل الترقية في مجلد `backups` بجوار القاعدة، ثم يطبع نتيجة التحقق دون عرض بيانات العملاء.

## فحص ما قبل النشر

```bash
npm run test:finance
npm run test:maintenance-contract
```

لا تُضاف `storage.json` أو ملفات النسخ الاحتياطية إلى Git؛ تبقى على القرص الدائم أو في موقع نسخ احتياطي آمن.
