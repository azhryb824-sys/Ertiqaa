# إعادة هيكلة النظام المالي والمحاسبي — وثيقة التصميم

> التاريخ: 2026-08-10 — المرحلة 2 من خطة إعادة الهيكلة المالية والمحاسبية.
> تبنى هذه الوثيقة على «تقرير شامل عن النظام» (SYSTEM_REPORT.md) وعلى فحص المرحلة 1 للكود الفعلي.

---

## 1) خلاصة الوضع الحالي (من المرحلة 1)

النظام المالي الحالي يعتمد على **قيود مفردة** (single‑entry) في مفتاح `misadFinancialEntries`, مع سجلات موازية لكل عملية:

| العملية | مفاتيح التخزين | المعالجة |
|---|---|---|
| تحصيل من عقد (تفعيل/سند قبض) | `misadFinancialEntries` + `misadReceipts` + `misadClaims` | قيد `type:"sale"` اتجاه `in` + سند قبض + مستخلص `receiptEntryId` |
| مصروف عقد | `misadContractExpenses` + `misadFinancialEntries` | سند صرف PDF من `contractExpenses` |
| فاتورة شراء مورد | `misadPurchaseInvoices` + `misadSupplierFinance` | دفعات داخل `payments[]` مع `paid` |
| راتب/مسير | `misadPayrolls` + `misadFinancialEntries` | قيد `type:"salary"` مجمّع واحد لكل المسير |
| سلفة/بدل/خصم | `misadFinancialEntries` | قيود `advance/allowance/deduction` مفردة |
| عهدة | `misadCustodies` + `misadFinancialEntries` | إضافة (out) وخصم (in) |
| مشتريات موظف | `misadStaffPurchaseInvoices` + `misadStaffVouchers` + `misadFinancialEntries` | سند صرف `staff-payment-voucher` |
| خزينة/بنوك | `misadTreasury` + `misadBankAccounts` | حركات `opening/deposit/withdraw/transfer` معزولة |
| فواتير عملاء | `misadCustomerInvoices` | تحصيل داخل `payments[]` بـ `paid` |

### نقاط الضعف المحددة في المرحلة 1
1. **لا يوجد قيد مزدوج**: كل قيد يمثّل دفعة/حركة نقدي، وليس ترحيلاً محاسبياً (لا مدين/دائن).
2. **الخزينة والبنوك معزولة** عن `misadFinancialEntries` — رصيد الصندوق لا يتطابق مضموناً مع القيود.
3. **فواتير العملاء والموردين تُحصّل داخلياً** (`payments[]`) دون قيد مالي مركزي موثّق.
4. **مسير الرواتب**: قيد واحد مجمّع بدل سجلات لكل موظف، والعُهد تُخصم في القيد المجمّع لكن دون تتبّع منطقي لحساب «سلف/عهد الموظفين».
5. **لا دليل حسابات** ولا تصنيف موحّد بين الوحدات (العقد/العميل/المورد/الموظف).
6. **لا تقارير محاسبية أساسية**: أستاذ عام، ميزان مراجعة، قائمة دخل، ميزانية. التقارير الموجودة (finance-summary, treasury, customer-statement) تجميعية وعرض فقط.
7. التدقيق مقتصر على `logActivity` (نصّ حر) دون سجل تدقيق مالي قابل للتتبع.

---

## 2) المبادئ الحاكمة للتصميم

1. **بلا فقدان بيانات**: القيود القائمة تبقى وتُقرأ كما هي؛ النظام الجديد يعمل من فوقها (additive). لا حذف، لا ترحيل مهجور، لا إعادة كتابة `misadFinancialEntries`.
2. **بلا تكرار مزدوج**: المحرك المحاسبي الجديد «يستمد» من السجلات العملياتية ويبني دفتر القيد المزدوج `misadJournalEntries` كطبقة مشتقة ومرجعية، بحيث تبقى صفحة «القيود الموحّدة» مصدراً فريداً.
3. **قيد مزدوج متوازن دائماً**: لكل قيد مجموع مدين = مجموع دائن، مع أرقام حسابات من دليل الحسابات.
4. **عزل الشركات**: كل سجل يحمل `companyOwnerId` ويُفلتر بـ `sameCompany` مثل سائر النظام.
5. **تطبيق تدريجي**: كل مرحلة تُضيف طبقة دون تعطيل التقارير والنماذج الحالية، وتُختبر E2E قبل التالي.

---

## 3) دليل الحسابات المقترح (Chart of Accounts)

مفتاح تخزين جديد: `misadChartOfAccounts` — مصفوفة حسابات ببنية:

```js
{ id:"1100", name:"الصندوق (الخزينة النقدية)", type:"asset", nature:"debit", parent:"1000", children:[] }
```

الأكواد (نمط مكوّن من 4 خانات، قابل للتوسع):

| الكود | الاسم | النوع | الطبيعة |
|---|---|---|---|
| 1000 | الأصول | (مجموعة) | debit |
| 1100 | الصندوق (النقد) | asset | debit |
| 1200 | البنوك | asset | debit |
| 1201..1209 | حساب بنكي لكل `misadBankAccounts` | asset | debit |
| 1300 | ذمم العملاء (عملاء جاري) | asset | debit |
| 1310 | ذمم عملاء حسب العقد | asset | debit |
| 1320 | ذمم فواتير العملاء | asset | debit |
| 1400 | سلف وعهد الموظفين | asset | debit |
| 1410 | سلف الموظفين | asset | debit |
| 1420 | عهد الموظفين | asset | debit |
| 1500 | مخزون قطع الغيار | asset | debit |
| 1600 | أصول ثابتة (مصاعد/معدات) | asset | debit |
| 2000 | الخصوم | (مجموعة) | credit |
| 2100 | ذمم الموردين | liability | credit |
| 2200 | سلف/عائدات مستحقة للعملاء | liability | credit |
| 2300 | ضريبة مستحقة | liability | credit |
| 3000 | حقوق الملكية | equity | credit |
| 3100 | رأس المال | equity | credit |
| 3200 | جاري المالك | equity | credit |
| 3300 | أرباح/خسائر متراكمة | equity | credit |
| 4000 | إيرادات | revenue | credit |
| 4100 | إيرادات عقود الصيانة | revenue | credit |
| 4200 | إيرادات عقود التركيب | revenue | credit |
| 4300 | إيرادات فواتير العملاء | revenue | credit |
| 4400 | إيرادات أخرى | revenue | credit |
| 5000 | مصروفات | expense | debit |
| 5100 | رواتب وأجور | expense | debit |
| 5200 | مشتريات وتكاليف عقود | expense | debit |
| 5300 | مصروفات تشغيل | expense | debit |
| 5400 | مصروفات عامة وإدارية | expense | debit |
| 5500 | خصومات ومنح | expense | debit |

إعداد افتراضي: عند أول تفعيل يُنشأ دليل الحسابات الافتراضي إن لم يوجد، ويُحدَّث ديناميكياً (حساب بنكي عند إضافة بنك، حساب سلف عند أول سلفة…) — محجوز داخل مرحلة 3.

---

## 4) دفتر القيد المزدوج (Journal)

مفتاح تخزين جديد: `misadJournalEntries` — كل قيد:

```js
{
  id:"JRN-<millis>",
  companyOwnerId:"OWN-...",
  date:"2026-08-10",            // تاريخ العملية
  description:"سند قبض RCT-... مقابل عقد CN-...",
  refType:"receipt",            // receipt | payroll | custody | advance | purchase-invoice | staff-voucher | customer-invoice | treasury-move | expense | allowance | deduction | opening | manual
  refId:"RCT-...",
  lines:[
    { account:"1100", accountName:"الصندوق", amount:6000, side:"debit"  },
    { account:"4100", accountName:"إيرادات عقود الصيانة", amount:6000, side:"credit" }
  ],
  createdBy:session.id, createdAt:"...", createdAtMs:Date.now()
}
```

القواعد:
- `totalDebit === totalCredit` إجبارياً؛ أي محاولة ترحيل غير متوازنة ترفض.
- `lines.length >= 2`.
- المرجع الداخلي `refType+refId` يضمن عدم الازدواج؛ إعادة تشغيل أي عملية مخصومة تُبقي القيد واحداً.
- قابل للاسترجاع من أي سجل عملياتي عبر `accountingFor(refType,refId)`.

---

## 5) قواعد الترحيل (مصفوفة العمليات)

| العملية | المدين (debit) | الدائن (credit) | المصدر |
|---|---|---|---|
| سند قبض / تحصيل عقد (نقداً) | 1100 الصندوق | 4100/4200 إيرادات | `misadReceipts` أو `financialEntries type:sale` |
| سند قبض بتحويل بنكي | 120x البنك | إيراد | paymentMethod |
| إضافة عهدة للموظف | 1420 عهد الموظفين | 1100 الصندوق | `misadCustodies` |
| خصم عهدة من الراتب | 5100 الأجور | 1420 عهد الموظفين | `custody-deduct` / مسير |
| سلفة موظف | 1410 سلف الموظفين | 1100 الصندوق | `financialEntries type:advance` |
| بدل (مستحق للموظف) | 5100 الأجور | 2200 مستحقات | `type:allowance` |
| خصم من الموظف | 2200 مستحقات | 5100 الأجور | `type:deduction` |
| مسير رواتب (دفع) | 5100 الأجور | 1100 الصندوق | `misadPayrolls` |
| فاتورة شراء مورد | 1500/5200 المخزون/تكاليف | 2100 ذمم الموردين | `misadPurchaseInvoices` |
| سداد مورد (سند صرف) | 2100 ذمم الموردين | 1100 الصندوق | دفعات `payments[]` |
| فاتورة عميل | 1310/1320 ذمم العملاء | 4300 إيراد فواتير | `misadCustomerInvoices` |
| تحصيل فاتورة عميل | 1100 الصندوق | 1320 ذمم فواتير العملاء | `payments[]` |
| مصروف عقد (سند صرف) | 5200 تكاليف | 1100 الصندوق | `misadContractExpenses` |
| سند صرف مشتريات موظف | 5200 تكاليف | 1100 الصندوق | `misadStaffVouchers` |
| حركة خزينة إيداع/سحب بنك | 1100/120x | 120x/1100 | `misadTreasury` |
| تحويل بين البنوك | 120x إلى | 120x من | `misadTreasury` |

يُبنى محرك `postFromSource(refType, refId)` في المرحلة 4 ويربطه بأزرار العملية القائمة (بلا حذف أي سلوك حالي).

---

## 6) التكامل مع الوحدات (الملفات المالية الموحّدة)

### أ) الملف المالي للعقد / العميل
- حساب «العميل» = مجموع قيود `1180/1310` المرتبطة بـ `contractId/clientId`.
- تُدمج بيانات `misadFinancialEntries` (سندات/مستخلصات) مع دفتر القيد المزدوج في صفحة الملف المالي نفسها.
- إضافة تبويب **«الأستاذ المحاسبي للعقد»** داخل `contractFinancePage` يعرض قيود العقد المرحّلة.

### ب) الملف المالي للموظف
- `staffFinanceInfo` تُوسّع بقيود مزدوجة تبين أثر كل سلفة/رهدة/راتب على حساب الموظف.
- حساب «الصافي» يبقى كما هو (لا تغيير في المعادلة) لكن يُضاف عمود «القيد المزدوج».

### ج) الملف المالي للمورد / المخزون
- `supplierFinanceInfo` تُعبّر عن ذمم الموردين من دليل الحسابات 2100 بدل `total-paid` فقط.
- مشتريات المخزون تُرحّل إلى 1500 عند تسجيل فاتورة شراء (اختياري: عند خروج قطع الغيار).

### د) الخزينة والبنوك
- `treasuryState` تبقى كمحرك عرض، ويُضاف لها قيد مزدوج عند كل حركة (فتحة `account` تُربط بحساب 120x/1100).

---

## 7) التقارير المحاسبية PDF (pdfmake)

مستندات جديدة داخل `pdfmake-gen.js` (دوال بصيغة `xxxPdfDefinition`) تُضاف إلى `window.generatePdf` عبر حالات `type === '...'`:

| المستند | type | المحتوى |
|---|---|---|
| دفتر الأستاذ العام | `ledger` | كل قيد مزدوج بحساباته ومرجعه |
| ميزان المراجعة | `trial-balance` | رصيد كل حساب، مدين/دائن |
| قائمة الدخل | `income-statement` | إيرادات - مصروفات = صافي |
| الميزانية العمومية | `balance-sheet` | أصول = خصوم + حقوق ملكية |
| كشف حركة حساب | `account-statement` | حركات حساب واحد (1100/2100/…) |

تُستخدم نفس البنية الحالية (`makeDd`, `appendDocumentHeader`, `safeFooter`) وتُنشأ الأزرار داخل تبويب «التقارير المالية» (المرحلة 8).

---

## 8) الصلاحيات والتدقيق

- صفحة «التقارير المحاسبية» والملفات المالية تبقى مقصورة على `owner/company_admin/admin`.
- كل قيد مزدوج يحمل `createdBy`؛ ويُضاف `misadFinanceAuditLog` يسجل: العملية، المرجع، القيم، المستخدم، التوقيت، والمجموع قبل/بعد (df/0/إضافة خفيفة لا تؤثر على الأداء).
- تدقيق الملفات المالية: زر «سجل تدقيق» لكل قيد يعرض من أنشأه ومتى وقيمته.

---

## 9) الهجرة والتراجع (Safety)

1. أداة تثبيت `ensureAccountingKeys()` تُنشئ المفاتيح الفارغة عند التحميل إن لم توجد (مثلما تفعل بقية المفاتيح).
2. `pendingJournal(refType,refId)` تُستدعى على السجلات القائمة مرة واحدة لبناء دفتر القيود من البيانات التاريخية (report‑only، لا تمسّ القيود القديمة).
3. كل مرحلة تُختبر ببروب E2E في `%TEMP%\opencode\` على قاعدة `prod-repro` ومفتاح `PORT` فريد، مع `JS ERRORS: []`.
4. عند أي شك، تُراجع مخرجات دالة قبل كتابتها، ولا تُحذف أي دالة قائمة.

---

## 10) خطة التنفيذ (مراحل 3–11)

- **3**: مفاتيح `misadChartOfAccounts` + `misadJournalEntries` + `ensureAccountingKeys` + توليد الدليل الافتراضي.
- **4**: `postJournal()` + مصفوفة الترحيل `postFromSource()` لسند القبض، العهدة، السلفة، الراتب، فواتير الموردين، فواتير العملاء، الخزينة.
- **5**: تبويب «الأستاذ المحاسبي للعقد» + ربط ملف العميل؛ تكامل صفحة «القيود الموحّدة» مع الدفتر.
- **6**: توسعة `staffFinanceInfo` بقيد مزدوج + مسير راتب بقيد لكل موظف.
- **7**: ذمم الموردين + ربط المخزون بالمشتريات وسندات الصرف.
- **8**: التقارير المحاسبية PDF (الأستاذ العام، ميزان المراجعة، قائمة الدخل، الميزانية).
- **9**: `misadFinanceAuditLog` + أزرار تدقيق + قوالب الصلاحيات.
- **10**: بروب E2E لكل تدفق + ريغريشن للمالية والموظفين والعقود.
- **11**: رفع `app.js?v=...` + `sw.js` + commit جديد.